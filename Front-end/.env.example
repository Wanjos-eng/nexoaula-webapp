# Origem da API (backend FastAPI).
# Usado pelo rewrite do Next.js para encaminhar /api ao backend.
# O navegador nunca acessa essa URL diretamente; todas as chamadas
# passam por /api na mesma origem (ver next.config.ts).
NEXT_PUBLIC_API_URL=http://localhost:8000
